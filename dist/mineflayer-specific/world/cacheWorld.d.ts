import { Vec3 } from 'vec3';
import type { World as WorldType } from './worldInterface';
import { Bot } from 'mineflayer';
import { LRUCache } from 'lru-cache';
import { Block, BlockType, MCData, RayType } from '../../types';
import { AABB } from '@nxg-org/mineflayer-util-plugin';
import { Movement } from '../movements';
export declare class BlockInfo {
    readonly replaceable: boolean;
    readonly canFall: boolean;
    readonly walkthrough: boolean;
    readonly safe: boolean;
    readonly physical: boolean;
    readonly liquid: boolean;
    readonly climbable: boolean;
    readonly height: number;
    readonly openable: boolean;
    readonly position: Vec3;
    readonly type: number;
    readonly block: Block | null;
    static initialized: boolean;
    static readonly interactableBlocks: Set<unknown>;
    static readonly blocksCantBreak: Set<number>;
    static readonly blocksToAvoid: Set<unknown>;
    static readonly climbables: Set<unknown>;
    static readonly carpets: Set<unknown>;
    static readonly fences: Set<unknown>;
    static readonly replaceables: Set<number>;
    static readonly liquids: Set<unknown>;
    static readonly waters: Set<unknown>;
    static readonly gravityBlocks: Set<unknown>;
    static readonly openable: Set<unknown>;
    static readonly emptyBlocks: Set<unknown>;
    static readonly scaffoldingBlockItems: Set<number>;
    static readonly mlgItems: Set<number>;
    static readonly INVALID: BlockInfo;
    static PBlock: BlockType;
    static _waterBlock: Block;
    static _solidBlock: Block;
    static _airBlock: Block;
    static _replaceableBlock: Block;
    static soulsandId: number;
    static substituteBlockStateId: number;
    additionalLoaded: boolean;
    private _waterAround;
    private _fallBlockOver;
    get waterAround(): boolean;
    get fallBlockOver(): boolean;
    get dY(): number;
    readonly solidFull: boolean;
    readonly isInvalid: boolean;
    constructor(replaceable: boolean, canFall: boolean, walkthrough: boolean, safe: boolean, physical: boolean, liquid: boolean, climbable: boolean, height: number, openable: boolean, position: Vec3, type: number, block?: Block | null);
    static init(registry: MCData): void;
    static fromBlock(b: Block | null): BlockInfo;
    static SOLID(pos: Vec3): BlockInfo;
    static AIR(pos: Vec3): BlockInfo;
    static REPLACEABLE(pos: Vec3): BlockInfo;
    static WATER(pos: Vec3): BlockInfo;
    getBBs(): AABB[];
    loadAdditionalInfo(movement: Movement): void;
}
export declare class CacheSyncWorld implements WorldType {
    blockInfos: LRUCache<string, BlockInfo>;
    world: Bot['world'];
    cacheCalls: number;
    enabled: boolean;
    minY: number;
    constructor(bot: Bot, referenceWorld: Bot['world']);
    private makeLRUCache;
    raycast(from: Vec3, direction: Vec3, range: number, matcher?: ((block: Block) => boolean) | undefined): RayType | null;
    getBlock(pos: Vec3): Block | null;
    getBlockInfo(pos: Vec3): BlockInfo;
    getBlockStateId(pos: Vec3): number | undefined;
    getCacheSize(): string;
    clearCache(): void;
    setEnabled(enabled: boolean): void;
    cleanup(): void;
}
